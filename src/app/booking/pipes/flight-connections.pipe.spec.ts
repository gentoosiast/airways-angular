import { FlightConnectionsPipe } from './flight-connections.pipe';

describe('FlightConnectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightConnectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
